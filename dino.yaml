oscar:
    log_level: 'DEBUG'
    storage:
        # mock, redis, cassandra
        type: 'cassandra'
        host:
            - '127.0.0.1'
    queue:
        type: 'redis'
        host: 'redis://localhost:6379'
    auth:
        type: 'allowall'
        host: 'localhost:6379'

dev:
    redis:
    log_level: 'DEBUG'
    storage:
        type: 'redis'
        host: 'maggie-kafka-3:6379'
        max_history: 200
    queue:
        type: 'redis'
        host: 'redis://maggie-kafka-3:6379'
    auth:
        type: 'redis'
        host: 'localhost:6379'

test:
    log_level: 'DEBUG'
    storage:
        type: 'redis'
        host: 'localhost:6379'
        max_history: 200
    queue:
        type: 'redis'
        host: 'redis://localhost:6379'
    auth:
        type: 'allowall'
