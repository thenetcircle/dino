oscar:
    log_level: 'DEBUG'
    database:
        type: 'redis'
        host: 'localhost:6379'
        db: 1
    storage:
        # mock, redis, cassandra
        type: 'cassandra'
        host:
            - '127.0.0.1'
    queue:
        type: 'redis'
        host: 'redis://localhost:6379'
        db: 2
    auth:
        type: 'redis'
        host: 'localhost:6379'
        db: 0
    cache:
        type: 'redis'
        host: 'localhost:6379'
        db: 3

dev:
    log_level: 'DEBUG'
    database:
        type: 'redis'
        host: 'maggie-kafka-3:6379'
        db: 1
    storage:
        type: 'redis'
        host: 'maggie-kafka-3:6379'
        max_history: 200
        db: 2
    queue:
        type: 'redis'
        host: 'redis://maggie-kafka-3:6379'
        db: 3
    auth:
        type: 'redis'
        host: 'localhost:6379'
        db: 0

test:
    testing: True
    log_level: 'DEBUG'
    database:
        type: 'redis'
        host: 'maggie-kafka-3:6379'
        db: 1
    storage:
        type: 'mock'
        host: 'localhost:6379'
        db: 0
        max_history: 200
    queue:
        type: 'mock'
        host: 'redis://localhost:6379'
        db: 2
    auth:
        type: 'allowall'
